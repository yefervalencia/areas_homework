const pi = 3.1416;
const r = prompt("Ingrese el radio del círculo: ");

const perimetrocirculo = 2 * pi * r;

console.log("El perímetro del círculo es: "+ perimetrocirculo);
alert("El perímetro del círculo es: "+ perimetrocirculo);